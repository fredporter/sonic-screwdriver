# uHOME Launcher Assets

`uhome-console.sh` is the source launcher artifact for the Linux-side
controller-first `uHOME` surface. The installer manifest stages it to
`/opt/uhome/bin/uhome-console` and calls it with module selectors such as
`home`, `library`, and `settings`.
