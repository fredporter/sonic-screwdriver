# Windows Launcher Assets

These `.cmd` files are the source launcher artifacts for the Windows 10 gaming
surface. The standalone Sonic repo stages them into
`C:\ProgramData\Sonic\Navigation\` so the controller-first shell can open the
home, library, and settings modules.
