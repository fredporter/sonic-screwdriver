# Sonic Memory

`memory/` holds local mutable state for Sonic runtime operations.

Tracked source code and tracked release metadata do not belong here.

See `memory/sonic/README.md` for the Sonic-specific layout contract.
