# Sonic Library

`library/` is reserved for locally installed or cloned bolt-on content that can
sit beside Sonic without becoming part of the tracked repo history.

Use `library/sonic/` for Sonic-managed integrations when needed.
